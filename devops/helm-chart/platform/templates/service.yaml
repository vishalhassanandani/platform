apiVersion: v1
kind: Service
metadata:
  name: {{ include "helloworld-chart.fullname" . }}
  labels:
    {{- include "__service_name__.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "__service_name__.selectorLabels" . | nindent 4 }}
