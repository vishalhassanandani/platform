kubernetes:
  host: https://kubernetes.default

helloworld:
  namespace: __stackname__
  image:
    name: __registryhost__/__image_name__
    tag: __chart_version__
    pullPolicy: IfNotPresent
  service:
    ingressClass: internal-ingress
